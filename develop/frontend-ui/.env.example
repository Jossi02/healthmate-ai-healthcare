# Public backend URL used by the browser. Never put credentials in NEXT_PUBLIC_* values.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080

# Compatibility alias. NEXT_PUBLIC_BACKEND_URL takes precedence when both are set.
# NEXT_PUBLIC_API_URL=http://localhost:8080
